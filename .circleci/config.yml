version: 2.0
jobs:

  #=======prebuild========# 
  #  Clone code from git hub, Running format-code, license, spelling check
  prebuild:
    docker:
      - image: bingyuliu/sample_gdesign:0.2.1
    working_directory: ~/ColorfullTime-Bingyu
    steps:
      - checkout
      # Build, Test and Install
      - run:
          name: Prebuild
          command: |
             echo "prebuild..." &&
             pwd &&
             ls -al &&
             echo "Prebuild Done!" 
      - save_cache:
           key: gdesign-sourceCode-cache
           paths:
             - ./../ColorfullTime-Bingyu        

  #========Build-Test-Install==========# 
  build-test-install-simulate-debug:
    docker:
      - image: bingyuliu/sample_gdesign:0.2.1
    working_directory: ~/ColorfullTime-Bingyu
    steps:
      - restore_cache:
          keys:
            - gdesign-sourceCode-cache
      # Build, Test and Install
      - run: 
          name: BuildSimuateDebug
          command: ./.circleci/externalscript "BuildSimuateDebug"
      - run: 
          name: TestSimuateDebug
          command: |          
            echo "Testing..." &&
            echo "Testing Done!"
      - run: 
          name: InstallSimuateDebug
          command: |
            ./.circleci/externalscript "InstallSimuateDebug" &&
            echo "Installation Done!"
      - save_cache:
           key: gdesign-sourceCode-cache-build-simulate-debug
           paths:
             - ./../ColorfullTime-Bingyu  
             


    docker:
      - image: bingyuliu/sample_gdesign:0.2.1
    working_directory: ~/ColorfullTime-Bingyu
    steps:
      - restore_cache:
          keys:
            - gdesign-sourceCode-cache
      # Build, Test and Install
      - run: 
          name: BuildRelease
          command: |
            echo "Building..." &&
            echo "Buildig Done!" 
      - run: 
          name: TestRelease
          command: |          
            echo "Testing..." &&
            echo "Testing Done!"
      - run: 
          name: InstallRelease
          command: |
            echo "Installing..." &&
            echo "Installation Done!"
      - save_cache:
           key: gdesign-sourceCode-cache-build-lby1-fls-release
           paths:
             - ./../ColorfullTime-Bingyu
     

workflows:
  version: 2
  build-test-install:
    jobs:
      - prebuild
      #  prebuild-->build-simulate-debug
      - build-test-install-simulate-debug:
          requires:
            - prebuild
      
